# CS5200 Lab 6 Invoice

SQL schema/data setup and a Python invoice query exercise.

`demo_invoice.py` reads MySQL credentials from environment variables:

```sh
MYSQL_USER=root
MYSQL_PASSWORD=your-local-password
```
