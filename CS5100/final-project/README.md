# Heart Disease Calibration Project

## How to Run
1. Install dependencies: `pip install -r requirements.txt`
2. Run `1_data_prep.ipynb` to process the data.
3. Run `2_model_calibration.ipynb` to train models and generate plots.